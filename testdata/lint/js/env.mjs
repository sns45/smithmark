// env.mjs is a capability lint fixture (task 4.1): every line here is a
// genuine match for the env detection class table (spec 9).

const apiKey = process.env.API_KEY;
console.log(process.env.NODE_ENV);
