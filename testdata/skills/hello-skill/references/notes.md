# Notes

This reference file is not a script and carries no special mode; it exists
to prove the walker treats every non SKILL.md file as a skill.scripts entry,
regardless of which subdirectory holds it.
