name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: ["**"]

jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: gofmt check
        if: matrix.os == 'ubuntu-latest'
        run: gofmt -l . | tee /dev/stderr | wc -l | grep -q '^0$'

      - name: go vet
        run: go vet ./...

      - name: go test
        run: go test ./...

      # Static validation of the release config, so a broken .goreleaser.yaml is
      # caught on every PR rather than only at tag time. Run via the ephemeral
      # go run form Task 2.9 used, so no separate action install is needed;
      # ubuntu only, since the check is platform independent.
      - name: goreleaser check
        if: matrix.os == 'ubuntu-latest'
        run: go run github.com/goreleaser/goreleaser/v2@latest check

  # Proves the wasip1 carried constraint (spec 2.1): the signing layer's stub
  # compiles under GOOS=wasip1 and sigstore-go stays out of the wasip1
  # dependency graph. sigstore-go does not compile for wasip1, so if it ever
  # leaked past the //go:build !wasip1 tag this build would fail. Ubuntu only;
  # the check is platform independent, so one runner suffices.
  wasip1-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: wasip1 build check
        run: GOOS=wasip1 GOARCH=wasm go build ./pkg/...
